function inchToFeet(num) {
    result = num / 12;
    return result;
}
var r = inchToFeet(100);
console.log(r)


// 'const' and 'let' 
// we will use const as var if we do not change that variable in future. like this will be fixed
// we will use let as var it means that we can change it in future.