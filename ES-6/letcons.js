// we will use const when we need a fix variable, we will not change the value of that variable
const name = 'shuvro baset';

const numbers = [23,75];
// if we use a list we can add some new value. 
numbers.push(7);
console.log(numbers);


// use let function
let fName = 'baset';
fName = 'shuvro';
console.log(fName);
